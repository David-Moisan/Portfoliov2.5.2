import React from 'react'

/**
 * BigBackgroundFont
 * @param {string} text 
 * @returns Un text en background
 */
export default function BigBackgroundFont({text}) {
    return (
        <div className='big-font'>
            {text}
        </div>
    )
}
