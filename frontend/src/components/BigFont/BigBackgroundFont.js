import React from 'react'
import { BigFont } from './BigBackgroundFontStyle'

/**
 * BigBackgroundFont
 * @param {string} text
 * @returns Un text en background
 */
export default function BigBackgroundFont({ text }) {
    return <BigFont>{text}</BigFont>
}
